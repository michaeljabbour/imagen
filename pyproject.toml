[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "imagen-mcp"
version = "0.4.0"
authors = [
  { name = "Michael J. Jabbour" },
]
description = "A Model Context Protocol (MCP) server for intelligent multi-provider image generation"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
# Runtime dependencies.
#
# The server uses the FastMCP implementation bundled inside the `mcp` SDK
# (`from mcp.server.fastmcp import FastMCP`), NOT the standalone `fastmcp`
# package. mcp >= 1.26 provides tool annotations, structured output,
# `ctx.elicit()`, `ctx.report_progress()`, and streamable-http transport,
# which is everything this server needs. The standalone `fastmcp` package
# was removed from the dependency set in v0.3.x because it was never
# imported (see MODERNIZATION_PLAN.md Phase 8).
dependencies = [
    "mcp>=1.26.0,<2",
    "pydantic>=2.12.3",
    "httpx>=0.28.0",
    "google-genai>=2.8.0",
    "pillow>=11.0.0",
]

[project.urls]
Homepage = "https://github.com/michaeljabbour/imagen-mcp"
Repository = "https://github.com/michaeljabbour/imagen-mcp.git"
Issues = "https://github.com/michaeljabbour/imagen-mcp/issues"
Changelog = "https://github.com/michaeljabbour/imagen-mcp/blob/v0.4.0/CHANGELOG.md"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "pytest-cov>=5.0",
    "respx>=0.21",
    "mypy>=1.10",
    "ruff>=0.9.0",
    "pre-commit>=3.0",
    "types-pillow>=10.2.0",
]

[project.scripts]
imagen-mcp = "imagen_mcp.server:main"
imagen-mcp-cli = "imagen_mcp.cli:main"

[tool.setuptools.packages.find]
include = ["imagen_mcp*", "src*"]

[tool.setuptools.package-data]
src = ["py.typed"]
imagen_mcp = ["py.typed"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]
ignore = []

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = false
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
strict_equality = true

[[tool.mypy.overrides]]
module = [
    "google.genai",
    "google.genai.types",
    "mcp.server.fastmcp",
    "mcp.types",
]
ignore_missing_imports = true

[tool.coverage.run]
source = ["src"]
branch = true
omit = [
    "src/cli.py",  # interactive CLI; exercised manually
]

[tool.coverage.report]
show_missing = true
fail_under = 80
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "if __name__ ==",
    "raise NotImplementedError",
    "\\.\\.\\.",
]
